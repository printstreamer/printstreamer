""" Format-agnostic processing layers: stats, identification, indexing, dump, merge.

Everything here operates on the generic model (or on an index file derived from it),
never on a specific print-stream format.
"""
