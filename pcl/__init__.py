""" A PCL5 parser that interprets the common escape-sequence subset (cursor
positioning, font selection, text, page breaks) into the generic model. HP-GL/2
vector mode and raster graphics are recognised but only partially modelled.
"""
